const ITEMS: [string, string][] = [
  ['Tapping Term / Auto Shift / Permissive Hold', '長押し判定の基準時間。Auto Shiftは長押しでShift文字、Permissive Holdは他キー併打でホールド確定。'],
  ['OS自動判別', '接続先OSを自動判定し、macOS/iOSならCmd/Ctrlを入れ替え。'],
  ['タップダンス', 'TD(0)〜TD(7)キーでタップ／ダブルタップ／長押しに別のキーを割当。ダブルタップ未設定ならタップと同じ動作。'],
  ['コンボ', '複数キー同時押しで別のキーを発動。トリガーキーは左から詰めて設定。'],
  ['DPIカーブ', '動かす速さに対する実際の速さを自由な曲線で調整。加速度設定より優先。'],
  ['自動マウス', 'トラックボールを動かすと自動でレイヤー切替、離すと一定時間後に戻る。'],
  ['スクロール／慣性スクロール', '指定レイヤーでトラックボールがスクロールに。慣性は弾いた時の余韻。'],
  ['ジェスチャー', 'ジェスチャーキーを押しながらボールを振って操作。RP2040版は4モードまで独立設定でき、レイヤー連動も可能。'],
  ['シェイク', 'ボールを素早く振ると発動。ジェスチャーキー不要で常時判定。'],
  ['ダブルフリック', '同じ方向へ短時間で2回フリックすると発動。カーソル操作とは独立して動作。'],
  ['精密モード', 'キーを押している間だけ感度を下げる。レイヤー連動も可能。'],
  ['レイヤー連動LED', 'レイヤーごとに専用の光り方を設定できる。'],
];

export function UsageGuide() {
  return (
    <div className="usage-guide">
      <p className="settings-desc" style={{ marginBottom: 12 }}>
        ※ 自動マウス・スクロール・精密モード・ジェスチャーは同じレイヤーに重複設定できません（精密モードとスクロール/自動マウスの組み合わせのみ例外）。
      </p>
      <dl className="usage-guide__list">
        {ITEMS.map(([term, desc]) => (
          <div className="usage-guide__item" key={term}>
            <dt>{term}</dt>
            <dd>{desc}</dd>
          </div>
        ))}
      </dl>
    </div>
  );
}
