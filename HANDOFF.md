# Keyball Link（Web版キーマップ設定ツール） — 引き継ぎメモ

> このファイルは「チャットをリセットしても／別デバイスに移っても作業を続けられるように」まとめた引き継ぎ書です。
> 次のセッションは、まずこのファイルと [README.md](README.md) を読んでから作業してください。

## 0. プロジェクト概要
- **何のアプリか**: Keyball（分割トラックボールキーボード）シリーズ用の、ブラウザから直接キーマップ編集・ファームウェア書き込みができるWebアプリ（WebHID / WebSerial API使用）。
- **アプリ名**: Keyball Link
- **技術**: React + TypeScript + Vite（インストール不要の静的サイト）
- **場所**: `~/keyball-configurator`
- **GitHub**: https://github.com/Yowkees/keyball-link （組織アカウント・Public。旧名 keyball-configurator は自動転送）
- **公開URL**: https://keyball-link.shiroganelab.com （Cloudflare Pages、プロジェクト名 `keyball-link`。DNSはShopify管理の shiroganelab.com にCNAME追加）
- **現在のアプリバージョン**: v1.2.1（`package.json`、footerに `Keyball Link v{__APP_VERSION__}` として表示）
- **対応機種**: Keyball39 / Keyball44 / Keyball61 / Keyball+

---

## 1. ユーザーについて（重要・厳守）
- プログラミング**初心者**。専門用語は必ず簡単な説明を添える。**日本語**で回答。
- **コミット/プッシュは毎回メッセージ案を提案**してから。実際の実行は「お願いします」等の明確な指示があったときだけ。
- 手動作業が必要なときは**ステップごとに**説明。
- ターミナル作業（npm / git 等）は**ClaudeがBashツールで実行**する。
- ユーザーはKeyball販売元「Shirogane Lab」の人間。

---

## 2. ファームウェアとの関係（重要）
このアプリは2つの**別リポジトリ**のファームウェアを配布・書き込みしている。**バージョン番号は連動していない**ので注意。

| 機種 | ファームウェアのリポジトリ | バージョン体系 |
|---|---|---|
| Keyball39 / 44 / 61 | https://github.com/Yowkees/keyball-link-firmware | 現在 v1.2.0 |
| Keyball+ | https://github.com/ineno771/keyball-plus-firmware （別リポジトリ。ローカルは `~/keyball-plus-firmware`） | 現在 v1.0.1 |

hexファイルの置き場所は `public/firmware/*.hex`。**hexを差し替えたら必ず `src/lib/firmwareFeatures.ts` の `LATEST_FW_VERSION`（機種別オブジェクト）も同じバージョンに更新すること**（更新し忘れると「最新版があります」の案内が正しく出ない）。

---

## 3. 直近の作業（2026-08-24〜25）
- Keyball+ファームウェアのLED点灯遅延バグ修正（v1.0.1）に伴い、hexを差し替え
- **既存バグ発見・修正**: `LATEST_FW_VERSION` が全機種共通の1つの定数になっており、Keyball+接続時に無関係なKeyball39/44/61系のバージョンと比較され「更新があります」と誤表示されていた → 機種別（`Record<ModelKey, FirmwareVersion>`）に変更（`src/lib/firmwareFeatures.ts` / `src/App.tsx`）
- README「対応キーボード」一覧にKeyball+が抜けていたのを追記
- CHANGELOG更新、`package.json` のバージョンを1.2.1に

本番デプロイは `npm run deploy` で実施可能（この開発環境はCloudflareにOAuthログイン済みなので、Claude環境からそのまま実行できる。※認証は途切れることがあるので、切れていたら `npx wrangler login` でユーザー自身のブラウザ認証が必要）。今回のv1.2.1（ドキュメント更新のみ）は**ユーザーの判断でデプロイ見送り**。次に機能・hexの変更を伴うコミットをする際は、デプロイの要否を確認すること。

### 2026-09-01: EEPROM設定バグ調査・修正一式（X投稿がきっかけ）
「Keyball Link対応ファームに書き換えたらレイヤーがおかしくなり、通常ファームに戻しても直らない」というX上の報告を受けて調査。原因は「EEPROMはファーム書き換えでは消えない」＋「設定の有効性判定が値の範囲チェックのみでマジックナンバーがなかった」こと。

- **keyball-link-firmware**（コミット`841c8ec`・push済み。**hexは未再ビルドで配布物には未反映、後述のタスク参照**）
  - `kb_settings.c`にマジックナンバー方式のEEPROM検証を追加。フォーマット不一致・未初期化時は独自設定領域を安全な既定値へ自動リセット（スクロールレイヤー等の既定値は正規ファームウェアと同じ挙動を維持するよう調整済み）
  - Keyball Linkの「初期化」ボタン（`KB_HID_CMD_RESET_KEYMAP`）の範囲を拡張。従来はキー割り当てのみだったが、Keyball Link独自設定（スクロールレイヤー等）と本体組み込み設定（CPI・スクロール分周・加速度・自動マウスレイヤーON/OFF）も含め、`EE_CLR`キーと同じ範囲をリセットするように
  - キーボード側への`EE_CLR`キー追加は**ユーザーの意向で見送り**（「キー割り当てはユーザーが決めること」のため。EEPROM初期化はKeyball Link側の「初期化」ボタンだけで完結する設計に統一）
- **keyball-configurator**（コミット`8480910`・デプロイ済み）
  - `src/lib/avr109.ts`の`exitBootloader()`が本来送るべき`0x45`('E')ではなく`0x51`('Q')を送信していたバグを修正。書き込み後すぐにアプリが起動しない（ブートローダーが8秒間抜けない）原因だった
  - `FirmwareFlasher.tsx`に「正規ファームウェアなど他のファームウェアに戻す方へ」の案内を追加。先に「初期化」を押してから書き込むよう誘導し、今回のような事故を予防する
  - ファームウェアの書き込み回数（全ユーザー合計）を表示する機能を追加（コミット`935f3f2`・デプロイ済み）。`functions/api/flash-count.ts`（Cloudflare Pages Functions）＋KVネームスペース`FLASH_COUNTS`（ID: `7c34554d9bca42a3b02465ce97bdd0b6`）で集計

---

## 4. 未確定・残タスク

### 2026-09-01の実機確認で判明した追加の不具合と対応状況
上記EEPROM修正の実機確認を進める中で、当初のEEPROM問題とは別に3件の不具合が見つかり、いずれも修正・push済み（実機で解消確認済みのものはチェック済み）。

- [x] **Keyball44/61 LED版のトラックボール方向が90度+反転していた**: ライブラリ標準の向き補正が、通常版のカスタム補正と反転条件が逆だった。通常版に合わせてLED版にも軽量な向き補正を追加（コミット`6f8240c`）。実機で解消確認済み。Keyball39は元々両版でコード共有のため対象外。
- [x] **自動マウスレイヤーとジェスチャーレイヤーが同じレイヤー番号になり得た**: Keyball Linkの「自動マウスレイヤーを使う」トグルだけがレイヤー重複チェックを素通りしていたのが原因。トグルにもチェックを追加（コミット`70ad5ae`）。
- [x] **マジックナンバー(EEPROM検証)が1バイトだと偶然の一致で誤判定する事故が実機で発生**: 2バイトに強化（コミット`8455233`所属の一連の修正、実際は`74feecb`）。
- [x] **hexファイル書き込み後「もう一度書き込む」でファイル選択が消える**: `reset()`がファイル選択まで消していたのを修正（コミット`0854073`）。実機確認済み（要ハードリロード）。
- [x] **画面上の「書き込み回数」表示が書き込み成功時に+1されるか**: 実機で確認済み。
- [x] **配布用hexの再ビルド・差し替え**: v1.3.0として`public/firmware/*.hex`を差し替え、`LATEST_FW_VERSION`・`kb_version.h`とも1.3.0に更新。本番デプロイ済み（コミット`791c667`、本番配信も確認済み）。
- [x] **呼吸エフェクト使用時、OLEDのスクロール分周・レイヤー表示が乱れる（解決・2026-09-10確認）**: 明るさ設定(val)を無視する仕様だったのをパッチで修正済み（`patches/0003-rgblight-breathing-respects-brightness.patch`、コミット`74feecb`+`8455233`）。当時は明るさを下げてもOLEDの乱れ自体は解消しなかったが、その後の作業（RGB_MATRIX移行等）で症状自体が発生しなくなったことを本人が確認。原因はいずれにせよ不明のまま解消。
- [ ] **アシストモード構想（アイデア段階、未着手）**: Keyballに慣れていないユーザー向けに、画面隅にキーマップを表示し続けるモード。レイヤーを切り替えると表示中のキーマップもレイヤーに追従して切り替わる。応用として、レイヤー切り替え時に音で知らせる機能も検討中（表示の切り替えと同じイベントに相乗りできるため実装コストは低い見込み）。
  - 技術的な前提: 現在のKeyball Link（設定時だけ接続する形）とは違い、**常時HID接続を維持してリアルタイムに状態（現在のレイヤー等）を受け取り続ける仕組み**が必要（ブラウザタブ常時起動 or 専用の常駐アプリ）。設計はまだこれから。
  - ファームウェア側の残タスクは `~/keyball-plus-firmware/HANDOFF.md` を参照（RGB_MATRIX移行・リポジトリ移管保留）。keyball-rp2040-firmwareの残タスクは `~/keyball-rp2040-firmware/HANDOFF.md` を参照。
- [x] **トラックボール超低速（精密作業）モードの感度設定UI**: `PRC_MO`キーコードをキー割り当てUIから選択可能にし、分周値（範囲2-5、既定4。ファーム側の実際の範囲に合わせて20→5に修正済み）・レイヤー連動の設定UIを`SettingsTab`に追加済み（コミット`935d441`→`b9f5644`→`ae50239`→`fbb5be6`）。rp2040-devブランチで完結。
- [x] **複数ジェスチャーモードの設定UI（2026-09-09・実機確認済み）**: ファーム側の4モード化（`~/keyball-rp2040-firmware/HANDOFF.md`参照）に対応し、`SettingsTab`のジェスチャーカードをモードタブ切り替え式に刷新。各モードで方向キー割り当て・方向ごとの連続入力ON/OFF・連動レイヤーを設定できる。`GST_HOLD2`〜`4`(0x7E13〜0x7E15)キーコードも追加。AVR版接続時や旧RP2040ファームでは新HIDコマンドの取得に失敗し、従来の単一モードUIに自動フォールバックする設計。rp2040-devブランチのみ（mainブランチ・AVR版は無変更）。
- [ ] **ポイント設置・スナップ機能構想（アイデア段階、未着手。別リポジトリで開始）**: 画面上の任意の箇所にポイントを登録しておき、そこへカーソルをスナップさせる機能。PC側常駐アプリ本体は `MagSnap`（旧keyball-companion。https://github.com/ineno771/mag-snap 、別デバイスで開発予定）として独立リポジトリ化済み。**Keyballとは無関係な独立製品として展開する方針**（対外的にKeyballへの言及はしない）。詳細・経緯はそちらのHANDOFF.md参照。
  - **ファームウェア単体では実現不可**: キーボードはUSBに対して相対移動量しか送っておらず、現在のカーソル絶対位置を知らないため。実現には**PC側の常駐ネイティブアプリ**が別途必要（ブラウザは仕様上カーソルを直接動かせないためWebアプリ不可）。役割分担は、キーボード側は「スナップ方向へ動いた」という合図を送るだけ、ポイント管理・実際のカーソル移動はPC側アプリが担当する想定。
  - 前述の「アシストモード」構想と同じく常駐アプリが前提になるため、実現するなら合わせて設計するのが効率的。
  - **配布コストの注意**: Mac版はApp Store経由でなくてもコード署名・公証のためApple Developer Program登録（年間$99）が実質必要（無署名でも配布自体は可能だが「開発元未確認」警告が出る）。Windows版は直接配布なら無料（署名なしはSmartScreen警告が出る。Microsoft Store経由なら一度きり約$19）。
- [x] **keyball-rp2040-firmware新機能に対応するWeb UI**: 汎用連続値調整機能（複数ジェスチャーモードの連続入力ON/OFF）・スクロール慣性の強さ調整・LED「波紋」演出（RIPPLE）は`SettingsTab`に実装済み。ジェスチャー連動LEDウェーブの速さ・ON/OFFトグルも実装済み（2026-09-10、`rp2040-dev`ブランチ）。詳細は`~/keyball-rp2040-firmware/HANDOFF.md`参照。
- [ ] **keyball-rp2040-firmware側の残る未着手アイデアに対応するWeb UI**: 矢印キーモード・軸スナップモード・方向別感度調整（8方向）・パイメニュー・OLEDリッチ化は、ファームウェア側がまだアイデア段階で未着手のため、Web UI対応も未着手（ファームウェア側の実装が先行する見込み）。

### 2026-09-11: OS自動判別・DPIカーブ・キーコード監査（`rp2040-dev`ブランチのみ、ビルド確認のみ・実機未確認・未コミット）
- [ ] **OS自動判別UI**: 接続中OSの判別結果表示＋macOS/iOS接続時にCmd/Ctrl自動入れ替えのON/OFFトグルを追加（`SettingsTab`）。ファーム側実装は`~/keyball-rp2040-firmware/HANDOFF.md`参照。
- [ ] **DPIカーブ編集UI**: `src/components/DpiCurveEditor/DpiCurveEditor.tsx`（新規）。Photoshopのトーンカーブ風に、SVG上の点をドラッグしてトラックボールの速度カーブを編集できる。ファーム側の実際の補間（モノトニック3次エルミートスプライン）と見た目を合わせるため、`computeDpiCurveLut()`（`protocol.ts`）でJS側にも同じLUT計算ロジックを移植済み。
- [x] **「この版（LED版）では使用できません」の誤表示を解除**: Tapping Term・Permissive Hold・Auto ShiftはRP2040版では制約がないのに、AVR版LED版向けの古い制限表示が残っていた。`SettingsTab.tsx`の`tappingUnavail`判定ごと削除。
- [x] **キーコード全数監査で発覚した不具合の修正**（`src/lib/keycodes.ts`）:
  - **ワンショットモディファイア(OSM)が反応しない不具合**: OSM(Ctrl/Shift/Alt/GUI)のキーコード値が誤り（`0x5501/02/04/08`→正しくは`0x52A1/02/04/08`）だった。**注意**: 既にこの誤った値でWeb UI経由保存済みのキーは、ファーム/Web更新だけでは直らない。ユーザーがそのキーを選び直して保存する必要がある。
  - **死んだキーコード`RGB_MODE_PLAIN`等(`0x782B`-`0x7834`)をパレットから削除**: 現行QMKに処理コードが存在しない値だった。`presets.ts`の「Keyball39 デフォルト」プリセットがこれを直接使っていたため、UG_PREV/UG_NEXTによる循環に置き換え済み（本人選択。輝度調整キーへの変更は試したが本人希望により見送り・元に戻し済み）。`via*`系プリセット（本家キーマップの機械的変換、意図的に無変更の方針）はそのまま。
- [x] **PC画面の明るさキーを追加**: 標準USB HIDキー`KC_BRIGHTNESS_UP`/`DOWN`(`0x00BD`/`0x00BE`)を「メディア」パレットに追加（ファーム側は`EXTRAKEY_ENABLE`が元々ONのため無変更）。OS・ディスプレイが対応している場合のみ有効。
- （一時実装→完全削除）OLED画面の明るさ調整UI: 「Val」の誤解から一時実装したが、本人が求めていたのはPC画面側だったため関連コード一式を削除済み。
- **重要**: 上記は全て`rp2040-dev`ブランチのみの変更。`npm run build`は通り、**`wrangler pages deploy dist --project-name=keyball-link --branch=rp2040-dev`で https://rp2040-dev.keyball-link.pages.dev には反映済み**（動作確認用途のデプロイ。originへのgit pushは行っていない）。一方で**ソースコードのgitコミットはまだ**（作業ツリーに変更が残ったまま）。次回セッションでまず`git status`（`rp2040-dev`ブランチにいることを確認）し、実機テスト後にコミットの要否を本人に確認すること。

### 2026-09-17: RP2040（UF2/BOOTSEL）書き込み対応を追加（`rp2040-dev`ブランチのみ、ビルド確認のみ・実機未確認・未コミット）
- [ ] **File System Access APIによるRP2040書き込み機能**: 既存のAVR版（WebSerial+AVR109、`.hex`）とは別に、RP2040版（`.uf2`、USBメモリ「RPI-RP2」への書き込み方式）を`FirmwareFlasher.tsx`に追加。
  - 新規`src/lib/uf2flash.ts`: `isFileSystemAccessSupported()`・`verifyUf2Drive()`（INFO_UF2.TXTの有無でRPI-RP2ドライブか確認）・`flashUf2()`（4096byte単位で書き込み、`close()`した瞬間にRP2040側が自動フラッシュ・自動再起動）。`window.showDirectoryPicker()`はTSのDOM libに型定義が無いため`uf2flash.ts`内でambient宣言を追加。
  - 新規`src/lib/deviceIds.ts`: AVR/RP2040のPIDテーブルを一元化（`KeyLayoutCards.tsx`にあった`MODEL_PIDS`をここに統合し、RP2040分`MODEL_PIDS_RP2040`を追加）。`KEYBALL_MODEL`値だけではAVR/RP2040を区別できない機種（Keyball+は両方139）があるため、判定は生の`productId`で行う（`chipForProductId()`）。
  - `FirmwareFlasher.tsx`: `chip: 'avr'|'rp2040'`のstateを追加し、接続中は`productId`から自動判定、未接続時は手動タブ選択。RP2040はLED版の分岐が無い（統一ビルド）ためLED版トグルは`chip==='avr'`時のみ表示。RP2040版ファームがまだ無い機種（Keyball44/61）を選んでいる間はRP2040タブをdisabledにし、選択中に機種を切り替えたら自動でAVRへ戻す。
  - `flashCount.ts`の`flashCountKey()`に`chip`引数を追加（デフォルト`'avr'`で既存呼び出しは無変更）。`functions/api/flash-count.ts`（Cloudflare Pages Functions）の`VALID_KEYS`にも`keyball39_rp2040`・`keyballplus_rp2040`を追加（**見落としやすい**: ここを更新し忘れると統計送信が400エラーになる）。
  - `public/firmware/keyball_{keyball39,keyballplus}_web_configurator.uf2`を`~/qmk_firmware/`からコピーして配置済み（手動・1回限り。`scripts/build-firmware.sh`のRP2040自動ビルド対応は今回のスコープ外・別タスク。専用QMK worktreeの新設が必要）。
  - **ビルド確認のみ完了**（`npm run build`成功、`wrangler pages deploy ... --branch=rp2040-dev`でプレビュー反映済み）。**実機（Keyball39 RP2040・Keyball+ RP2040）での書き込みテストはまだ行っていない**。次回セッションで優先的に確認すること（RPI-RP2ドライブ選択ダイアログ〜書き込み〜自動再起動〜統計加算までの一連の流れ、誤フォルダ選択時のエラー表示、Keyball44/61選択時のタブdisabled化）。
  - **【既知の不具合・未解決】HID経由の自動ブートローダー切り替え後、RPI-RP2ドライブが選択ダイアログに出てこない（本人の実機で再現）**: `onReboot()`（`KB_HID_CMD_REBOOT`送信→`bootloader_jump()`→`reset_usb_boot()`、QMKのRP2040標準実装で仕組み自体は正しいはず）を送った後、待ち時間を1.5秒→3.5秒に伸ばしても解消せず。原因未特定（OS側のマウント処理がそれ以上に遅い／`sendReport`のreport ID指定に問題がある／ファーム側で何か別の要因、等の可能性があるが未調査）。本人は当面**手動でのブートローダー切り替え（リセット2回押し／BOOTSEL+挿し直し）**で運用する方針に合意済み。自動切り替え自体を直す価値があるかは次回、実機ログ等で切り分けてから判断する。
  - RP2040タブの表示ラベルを「RP2040版（SparkFun Pro Micro RP2040）」→「RP2040版（RP2040 ProMicro）」に変更（本人指定）。
  - コミットはまだ実施していない（本人の明確な許可待ち）。

### 2026-09-17（続き）: 加速度スクロール・トラックボールリアクティブLED・ホールドタブUI改善（`rp2040-dev`ブランチのみ、ビルド確認のみ・実機未確認・未コミット）
- [x] **加速度スクロールは追加した後、本人の判断で全面削除**: 一度はファーム・Web UI両方に実装したが、実機確認前に「やっぱり不要」となったため`scrollAccel`関連のコードを全て削除した（`protocol.ts`/`hid.ts`/`useKeyball.ts`/`App.tsx`/`TrackballSettingsTab.tsx`/`ScrollLayerCard.tsx`）。詳細はファーム側HANDOFF.mdの同日エントリ参照。
- [x] **トラックボールリアクティブLEDの減衰時間延長**（ファーム側のみ、Web UIに変更なし）: `rgb_matrix_user.inc`の`TRACKBALL`エフェクトで、動きが止まってから元の色に戻るまでの時間が短すぎる（200〜900ms）と指摘があり、500〜3000msに延長。keyball39・keyballplus両方の`web_configurator`キーマップに適用済み、ビルド確認済み。
- [x] **キー設定パネルのCSSバグ修正**: 「特殊」カテゴリ（NONE/TRNS/BOOTの3キーのみ）のように候補が少ないと、グリッドの行が1段しかないせいで`align-content`の初期値`stretch`によりキーが縦に間延びしてしまう不具合を修正（`.modal-panel__grid--compact`に`align-content: start`を追加）。
- [x] **ホールドタブのレイアウトを「通常」タブと揃える**: 従来はパネル全体がスクロールする作りだったが、「通常」タブと同じく見出し・ホールド設定は常に表示したまま「タップ時のキー」一覧だけが残りの縦幅いっぱいに広がってスクロールするよう`builder-section--grow`クラスを追加し、`.modal-body.key-config-panel__body`の`overflow-y: auto`を`hidden`に戻した（内側のグリッドが自前でスクロールを持つようになったため）。
- [x] **ホールドタブに「ジェスチャー」「精密モード」を追加**: 修飾キー・レイヤー切替に加え、ホールド時にジェスチャーモード1〜4（`GST_HOLD`/`GST_HOLD2`〜`4`）または精密モード（`PRC_MO`）へ一時切替できるようにした。これらは256を超えるキーコードでMT()/LT()の8bitホールド値に収まらないため、既存の「タップダンス」機能（TD(0)〜TD(7)、`hold`欄が16bitフルで保持可能）を裏で流用する実装にした：選択すると未使用のTD枠を1つ自動確保し`{tap: 選んだキー, hold: GST_HOLD系/PRC_MO}`を書き込んでTD(n)を実際のキーへ配置する（`KeyConfigModal.tsx`の`HoldPanel`、`findFreeTdSlot`を`protocol.ts`に新設）。TD枠を使い切っていると赤字のエラーを出して既存キーを壊さないようにしてある。**この機能は右側常時表示のキー設定パネル（`App.tsx`の`KeyConfigPanel`）でのみ有効**——コンボ/ジェスチャー方向/シェイク/ダブルフリック/マクロステップ等の個別キー選択ポップアップ（`KeyConfigModal`）には`tdSlots`を渡していないため、意図せずTD枠を消費することはない。**実機での動作確認（TD経由でGST_HOLD/PRC_MOが正しく発火するか、既存キーの再編集で正しく状態復元されるか）はまだ行っていない**。
- 上記すべて`npm run build`成功・`wrangler pages deploy ... --branch=rp2040-dev`でプレビュー反映済み。コミットはまだ実施していない（本人の明確な許可待ち。この時点でRP2040 UF2書き込み機能分と合わせ、未コミットの変更がかなり蓄積している）。

### 2026-09-17（続き2）: ボール動作タブの保存ボタン統一・加速度カーブグラフの余白修正・16:9レターボックス化（`rp2040-dev`ブランチのみ、ビルド確認のみ・実機未確認・未コミット）
- [x] **「ボール動作」タブ内の「保存」ボタンを削除**: 中身を見ると右上の「保存」ボタンと全く同じ`handleSave`（`App.tsx`）を呼んでいるだけの重複だったため、`TrackballSettings.tsx`から削除し、`onSave`プロパティを`TrackballSettingsTab.tsx`・`App.tsx`の呼び出し経路から除去した。機能的な差はない。
- [x] **加速度カーブのプレビューグラフ上半分の余白を解消**: `DpiCurveEditor`のY軸上限は元々`DPI_CURVE_Y_MAX=255`固定だったが、「加速度」設定から算出されるプレビュー値は仕様上127までしか到達しない（`computeAccelCurvePoints`参照）ため、グラフの上半分が常に空白になっていた。`DpiCurveEditor`に`yMax`プロパティ（省略時255）を追加し、`TrackballSettings.tsx`の呼び出しで`yMax={127}`を渡すことで、カーブがグラフの縦幅いっぱいまで使うようにした。
- [x] **画面比率を16:9基準・横長時はレターボックス化**: `#root`の幅を`min(100vw, calc(100svh * 16 / 9))`にして`margin: 0 auto`で中央寄せした。ウィンドウが16:9より横長（超ワイドモニタ等）の場合は左右にbodyの背景色がそのまま見える余白ができ、16:9より縦長（通常のノートPC等）の場合は今まで通り画面幅いっぱいに広がる。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない（本人の明確な許可待ち）。

### 2026-09-17（続き3）: バージョン表示をLED設定の下に移動・慣性スクロール最大強度の縮小（`rp2040-dev`ブランチのみ、ビルド確認のみ・実機未確認・未コミット）
- [x] **ページ全体のフッター（`<footer className="app-footer">`）を廃止し、キーマップタブの「LED設定」パネルのすぐ下に移動**: `App.tsx`から`<footer>`要素を削除し、同じ内容（Keyball Linkのバージョン・接続中ファームのバージョン・更新案内）を`keymap-led-panel`の直後に`.keymap-version`として配置（`margin-top: -6px`で親のgap分を打ち消し、詰めて表示）。この表示はキーマップタブで接続中のときだけ見える（元のフッターは全タブ常時表示だったので挙動が変わる点に注意）。16:9のレターボックス化（`#root`の`width: min(100vw, calc(100svh*16/9))`）は`#root`内側の変更なので影響なし。
- [x] **慣性スクロールの「強さ」スライダーの最大値を254→15に縮小**（本人指摘: 「現状強すぎる」）。`protocol.ts`の`SCROLL_INERTIA_STRENGTH_MAX`/`DEFAULT`を15/8に変更。ファーム側で実際の最大強度自体も下げてある（詳細はファーム側HANDOFF.mdの同日エントリ参照）。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-17（続き4）: 16:9レターボックスが効かない不具合修正・バージョン表示位置の再修正・キー未選択時のホールド/カスタムタブに案内トースト追加（`rp2040-dev`ブランチのみ、ビルド確認のみ・実機未確認・未コミット）
- [x] **16:9レターボックスが表示されない不具合を修正**: `#root`の幅計算に`calc(100svh * 16 / 9)`を使っていたが、本人の環境では効いていなかった（幅いっぱいに広がったままだった）。`svh`単位がその環境で未対応/無効になり`min()`全体が無効値としてブラウザに無視され、結果`width`指定自体が効いていなかった可能性が高いと判断し、より広くサポートされている`vh`に変更（`min(100vw, calc(100vh * 16 / 9))`）。高さの指定(`height: 100svh`)自体は変更していない。**本人の環境での再確認が必要**。
- [x] **バージョン表示の位置を再修正**: 前回「LED設定のすぐ下」に置いたが、実際にはLED設定パネル（左カラム）の下にしかならず、右側の「キー設定」パネルの下までは伸びていなかった。本人の指摘（「LED設定とキー設定の下に配置」）を受け、`.keymap-version`を2カラムのグリッド（`.keymap-grid`）の外側・`.keymap-view`直下に移動し、両カラムの下に全幅で表示されるようにした（`App.tsx`）。
- [x] **キー未選択のままホールド/カスタムタブをクリックした時に案内トーストを表示**: 従来はネイティブの`disabled`属性で単に無反応にしていたが、「キーを選択してください、と出ると親切」との提案を受け、`disabled`属性は外して見た目だけ`.tab--disabled`で維持しつつ、クリック時に`onDisabledTabClick`コールバック経由でトースト「先にキーマップ上のキーを選択してください」を表示するようにした（`KeyConfigModal.tsx`の`KeyConfigPanel`、`App.tsx`）。ついでにトーストの種別に`info`（青系、非エラー）を追加した（`App.tsx`の`Toast`型、`index.css`の`.toast--info`）。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-17（続き5）: 16:9幅を固定px化・保存ボタンの未保存インジケータを全設定に拡大・ホールドタブの見た目調整（`rp2040-dev`ブランチのみ、ビルド確認のみ・実機未確認・未コミット）
- [x] **16:9の幅計算をvh基準から固定px(1600px)に変更**: 本人から「バージョンの下の空白は計算に入れないでください」との指摘。実際のコンテンツ高さは画面の高さより短いことが多く（下に空白ができる）、vh基準の計算だとその空白まで含めてしまい横幅の上限が意図より広くなっていた。`#root`の`width`を`min(100vw, calc(100vh*16/9))`から`min(100vw, 1600px)`に変更（`index.css`）。
- [x] **右上の「保存」ボタンの未保存インジケータ（既存の`hasUnsaved`state）を、キーマップ編集以外の全設定変更にも適用**: 従来は`assignKey`/undo/redoなどキーマップ編集時にしか`setHasUnsaved(true)`が呼ばれておらず、トラックボール・LED・詳細設定・ジェスチャー各種・シェイク・ダブルフリック・コンボ・DPIカーブ・タップダンス・精密モード・慣性スクロール・レイヤー連動LED・マクロ・レイヤー並べ替えを変更しても未保存表示にならなかった。該当する`handleXxxChange`系ハンドラ全てに`setHasUnsaved(true)`を追加し、直接setterを渡していた4箇所（`onScrollInertiaChange`/`onPrecisionChange`/`onLayerLedEnableChange`/`onLayerLedChange`）は新設のラッパー関数（`handleScrollInertiaChange`等）経由に変更した（`App.tsx`）。
- [x] **バージョン表示をLED設定パネルのすぐ下から、2カラムグリッド全体の下（キー設定パネルも含む）に移動**: 前回はLED設定パネル（左カラム）の下にしか伸びていなかった。`.keymap-version`を`.keymap-grid`の外側・`.keymap-view`直下に移動（`App.tsx`）。
- [x] **ホールドタブの表示調整**:
  - 「ホールド時の動作」ボタン群の上にあった説明文（「タップ→通常キー、ホールド（長押し）→…」）を削除。
  - ホールド種別タブ（修飾キー/レイヤー切替/ジェスチャー/精密モード）とレイヤー選択ボタンで日本語テキストが折り返されていた不具合を修正（`white-space: nowrap`を追加し、代わりにコンテナ側に`flex-wrap: wrap`を付けて収まらない分は次の行へ）。
  - レイヤー選択ボタンが一部しか見えていなかった（`.builder-layer-buttons`に`flex-wrap`が無かった）のを修正し、全レイヤーが折り返して表示されるようにした。
- [x] **キー未選択時にホールド/カスタムタブをクリックした時の案内を、タブのすぐ下にも表示**: 従来はグローバルなトースト通知のみだったが、本人の「ホールドボタンの近くに出ると良い」との提案を受け、タブ直下に2.5秒だけ表示される案内文を追加（`KeyConfigModal.tsx`）。グローバルトーストは維持したまま並行して表示する。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-17（続き6）: 16:9レターボックスを撤回、ウィンドウ全体に広がる形に戻した上で余白を有効活用（`rp2040-dev`ブランチのみ、ビルド確認のみ・実機未確認・未コミット）
- [x] **16:9レターボックス化を完全に撤回**: 本人の判断で「やっぱりウィンドウ全体に広がるように」となったため、`#root`の`width`指定（`min(100vw, 1600px)`等）を削除し、単純に`width: 100vw`に戻した（`index.css`）。
- [x] **代わりに、ウィンドウが広い時の余白を2箇所で有効活用するようにした**:
  1. **キーマップパネル（キー配列図）を自動拡大**: `KeyboardLayout.tsx`に、表示先コンテナ（`.layout-scroll`）の実測幅をResizeObserverで監視し、キー配列の自然な幅(`maxX`)より広ければその比率でCSS `transform: scale()`をかけて拡大表示する仕組みを追加（最大1.5倍まで、狭い場合は縮小せず等倍のまま＝従来通り横スクロールに任せる）。絶対座標で組んでいる既存のキー配置ロジックには一切手を入れず、外側から丸ごと拡大するだけなので副作用が少ない。
  2. **キー設定パネルのキー一覧の列数を可変に**: `.keymap-grid`の列比率を`1fr 400px`（右側固定）から`minmax(0,4fr) minmax(400px,1fr)`に変更し、右側のキー設定パネルもウィンドウが広い時は一緒に広がるようにした。それに合わせて`.modal-panel__grid--compact`の列数を固定6列から`repeat(auto-fill, minmax(58px, 1fr))`に変更し、パネルが広がった分だけキーが並ぶ列数も自動的に増えるようにした（400px幅ではこれまで通り6列になるよう最小幅を調整済み）。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-17（続き7）: キーマップパネルの上下余白を拡大＆拡大表示と連動、キー一覧の表示行数への波及
- [x] **キー配列の上下余白を拡大し、拡大表示（scale）と同じ比率で一緒に大きくなるようにした**: `KeyboardLayout.tsx`に`EXTRA_VPAD_BASE = 40`を追加し、`.keyboard-layout-scale-box`の`marginTop`/`marginBottom`を`EXTRA_VPAD_BASE * scale`に設定（メイン表示のみ。`showDescBar=false`の全レイヤー一覧表示には適用しない＝窮屈にならないよう据え置き）。既存の`.layout-scroll`側の`padding: 44px 0`と合算されるので、等倍時は44+40=84px、1.5倍拡大時は44+60=104pxの余白になる。
- [x] **上記により左カラムの実測高さが増える分、右のキー設定パネル（`keymapLeftHeight`で高さを揃えている）も自動的に高くなり、キー一覧によりが多くの行が表示されるようになった**（コード変更は不要。既存の`flex:1`+`overflow-y:auto`の仕組みがそのまま効く）。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-17（続き8）: ウィンドウ幅の活用をさらに強化
- [x] **キー配列の拡大上限を1.5倍→2.2倍に緩和**（`KeyboardLayout.tsx`の`MAX_SCALE`）。広いウィンドウで拡大が早々に頭打ちになり、画面を使い切れていなかったため。
- [x] **`.keymap-grid`の列比率を4:1→3:1に変更**（`index.css`）。右側キー設定パネルにも少し多めに余白を回す。
- 上記`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-17（続き9）: ウィンドウ幅を超えてはみ出す不具合の修正・キー配列の拡大を抑えて余白優先に変更
- [x] **ウィンドウ幅を超えてはみ出す不具合を修正**: `#root`の`width: 100vw`が原因（縦スクロールバーの分だけ実際の表示幅より広く計算されることがある、よく知られたCSSの落とし穴）。`width: 100%`に変更し、念のため`body`にも`overflow-x: hidden`を追加（`index.css`）。
- [x] **キー配列の拡大上限を2.2→1.3に戻し、それ以上のウィンドウ幅の伸びはキーを大きくするのではなく余白を増やす形に変更**（本人指摘：「キーマップも大きすぎるので余白を取る形であまり大きくなりすぎないように」）。`KeyboardLayout.tsx`で、キー配列自体の拡大率(`scale`、`MAX_SCALE=1.3`で頭打ち)と、上下余白の拡大率(`rawScale`、頭打ちなし・`PAD_SCALE_CAP=3`で緩い上限のみ)を分離。ウィンドウが広い時、キー配列は1.3倍までしか大きくならないが、それ以上の余った幅・高さは上下の余白が増える形で吸収される。
- [x] **ジェスチャーのレイヤー0連動は本人確認により動作OK**（前回セッションの想定通り、単に最新ファームが未書き込みだった可能性が高い）。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-17（続き10）: ウィンドウを狭めた時にキー配列を縮小・LED設定とキー設定パネルの表示順を入れ替え
- [x] **ウィンドウが狭い時、キー配列を横スクロールに頼らず縮小して画面に収めるようにした**: `KeyboardLayout.tsx`で、これまで`Math.max(1, ...)`で1倍未満への縮小を禁止していたのを撤廃し、`MIN_SCALE = 0.5`を下限に自由に縮小できるようにした（それ以上狭い場合のみ`.layout-scroll`のoverflow-x:autoに任せる、従来通り）。
- [x] **1カラム表示（画面幅980px以下）になった時、表示順を「キー配列→キー設定パネル→LED設定」に変更**（本人要望。従来はキー配列→LED設定→キー設定パネルの順だった）。`.keymap-left-panel`（キー配列とLED設定をまとめる箱）にこの幅でのみ`display: contents`を適用して中の要素を`.keymap-grid`直下に「昇格」させ、`order`プロパティで3要素（キー配列カード・キー設定パネル・LED設定）を並べ替える形にした（`index.css`）。合わせて、2カラム時専用のJS高さ強制（`keymapLeftHeight`）をこの幅では`!important`で無効化し、自然な高さで積み重なるようにした。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-17（続き11）: 表示順入れ替えが効いていなかったバグ修正・LED設定のレイヤータブをドロップダウン化
- [x] **「LED設定が上に来ている」＝前回の表示順入れ替えが実際には効いていなかったバグを修正**: 原因はCSSのソース順。`@media (max-width:980px){ .keymap-left-panel{display:contents} }`を`.keymap-left-panel{display:flex;...}`という無条件の基本スタイルより**前**に書いてしまっていたため、同じ詳細度で後勝ちのCSSカスケードにより、メディアクエリの条件が成立していても基本スタイル（`display:flex`）の方が後着で上書きしてしまい、`display:contents`が一度も効かないまま無効化されていた。`.keymap-left-panel`等4つの基本スタイル定義より後ろに移動して解決（`index.css`）。**CSSでメディアクエリによる上書きを書くときは、対象セレクタの基本スタイル定義より後ろに置くこと**（今後also注意）。
- [x] **LED設定パネルのレイヤー選択タブが横にはみ出す不具合を修正**: `.led-panel__targets`に`flex-shrink:0`が付いており、`flex-wrap:wrap`があっても箱自体が縮まないため一度も折り返されず、常に自然幅のまま横にはみ出していた。修正ではなく、本人要望通り狭い時（980px以下）はタブの代わりにドロップダウン（`<select>`）に切り替える方式にした。`LedPanel.tsx`に`led-panel__target-select`を追加（既存のボタンタブと同じ選択肢・同じ`setTarget`ハンドラを共有し、CSSのメディアクエリでどちらか一方だけ表示を切り替える）。
- 上記すべて`npm run build`成功・プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-18: 細かいUI改善一式・テーマ初期値変更・タイピングヒートマップ速度対応
- [x] **キーマップ左右の最低余白を確保**（本人「ウィンドウのサイズによってはキーマップの下に横スクロールが表示される」）: `KeyboardLayout.tsx`の拡縮計算に使う「利用可能幅」自体から`SIDE_MARGIN_PX`(16px)×2を差し引くようにした。コンテナ幅ぴったりに合わせていたことによる端数はみ出しが原因だった。
- [x] **キー未選択時にホールド/カスタムタブを押すと出る案内メッセージを、画面下部の共通トーストからキー設定パネルの右隣にposition:fixedで浮かせる表示に変更**（本人要望）。パネルはページ最右列で右側の余白がページ外周パディング分しか無いため、横位置はページ右端基準の固定値にし、縦位置だけJS側でパネルの実座標を測って合わせている（`KeyConfigModal.tsx`の`KeyConfigPanel`、`index.css`の`.key-config-panel__side-hint`）。旧`onDisabledTabClick`トースト呼び出しと`Toast`型の`'info'`種別は不要になったため削除。
- [x] **ホールドタブの「複数選択でCtrl+Shiftなどの組み合わせにできます」、カスタムタブの「QMKのキーコードを16進数で直接入力します…上級者向け」の説明文をそれぞれ削除**（本人要望）。
- [x] **キーマップ上でキー以外の余白をクリックすると選択解除されるようにした**（本人要望）。`.layout-scroll`にonClickを追加し、クリック対象が`.key`の中でなければ`setSelectedKeyIndex(null)`。
- [x] **テーマの初期値をダーク→ライトに変更**（本人「web uiはライトモードがデフォルトにしてください」）: `localStorage`に保存が無い場合のフォールバック値を`'dark'`→`'light'`に（`App.tsx`）。既に切替・保存済みのユーザーはそちらが優先される。
- [x] **タイピングヒートマップの「速度」スライダーを有効化**（本人「どれだけのタイピングで赤くなるかの早さ調整をできるようにしてください」）: ファーム側`HEATMAP()`が`rgb_matrix_config.speed`を1打鍵あたりの熱量増加量に転用するよう変更したのに合わせ、`LED_NO_SPEED_EFFECT_IDS`からタイピングヒートマップ(15)を除外（`protocol.ts`）。ファーム側の詳細は`keyball-rp2040-firmware`のHANDOFF.md参照。
- 上記すべて`npm run build`成功・`rp2040-dev`プレビュー反映済み。コミットはまだ実施していない。

### 2026-09-18（続き）: 右隣ヒントが「表示直後に右へ飛ぶ」不具合を修正
- 本人「キーを選択していない状態でホールドを選んだ時のポップアップの挙動がおかしいです」→ヒアリングの結果「表示された後に右になぜか移動する」と判明。
- **原因**: `.key-config-panel__side-hint`の登場アニメーションに、画面下部トースト用の`toast-in`キーフレームをそのまま使い回していた。`toast-in`は`left:50%`とセットの`translateX(-50%)`を前提にしており、`right`基準で配置しているこのヒントに適用すると、アニメーション再生中はtranslateX(-50%)分だけ左にずれた位置に描画され、0.15秒のアニメーションが終わった瞬間に`transform`が既定値（無し）へ戻るため、見た目上「表示直後に右へスナップして飛ぶ」ようになっていた。
- [x] 横方向には動かさない専用の`@keyframes side-hint-in`（`translateY`のみ）を新設し、`toast-in`の使い回しをやめた（`index.css`）。
- `npm run build`成功・`rp2040-dev`プレビュー反映済み。

### 2026-09-18（続き2）: 「LED位置実測（開発用）」がKeyball+で55番目まで確認できないバグを修正
- keyball-rp2040-firmware側の「Keyball+のLED一部消灯」調査（詳細はそちらのHANDOFF参照）の過程で発覚。`SettingsTab.tsx`の`ledCount`props（LED位置実測パネルの上限インデックス）が呼び出し元（`App.tsx`）から一切渡されておらず、常にデフォルト値46（Keyball39用）になっていた。Keyball+は55個LEDがあるため、後半9個（46〜54番）を一度も検証できない状態だった。
- [x] `App.tsx`の`<SettingsTab>`呼び出しに`ledCount={state.model === 'keyballplus' ? 55 : undefined}`を追加。
- `npm run build`成功・`rp2040-dev`プレビュー反映済み。
- 本人が修正直後に確認した際「46個までしか確認できなかった」と報告があったが、これはビルド済みdistが既に最新（再ビルドしてもハッシュ一致）だったため、ブラウザ側のキャッシュが古かっただけと判明。ハード再読み込みで解消。

### 2026-09-18（続き3）: リリース統合に向けた棚卸し・ホールドタブのジェスチャー/精密モード機能を撤去・ファームウェア自動ビルドにRP2040対応・Keyball+のAVR版配布を停止
- 本人より「そろそろKeyball Linkリリース版と統合したい。他にやっておくべきことはあるか」と相談があり、棚卸しを実施。判明した主な論点：(1) 未コミットの変更が大量に蓄積（25ファイル変更＋4新規）、(2) `main`ブランチも独自に10コミット進んでおり単純なfast-forwardにできない、(3) `scripts/build-firmware.sh`がRP2040の自動ビルドに未対応で毎回手動コピーが必要、(4) RP2040の自動ブートローダー切替後にRPI-RP2ドライブが出てこない既知の不具合。
- **(4)への対応**: 本人が実機で確認済み（「試しました。手動でブートモードにする必要がありましたが書き込みはできました。現状はこの方法で行きます」）。自動切替の修正は見送り、手動リセット運用を継続する方針で確定。
- **ホールドタブの「ジェスチャー」「精密モード」機能を撤去**: 前回セッションでタップダンス枠(TD)を裏で流用する実装を追加していたが、本人が実機で試したところ「設定しようとするとTD0が設定されてしまいます」と報告。切り分けの結果、これは「1つ目のキーではTD0になるのは正常（空き枠の自動確保）だが、2つ目の別キーに設定しようとしても同じTD0を奪い合って1つ目の設定を上書きしてしまう」という実際の不具合だった。GST_HOLD/GST_HOLD2〜4・PRC_MOはいずれも「通常」タブから単純なタップキーとしても選択できる設計のため（`keycodes.ts`のKEYCODES参照）、本人の判断で「ジェスチャー・精密モードはタップのみで使える仕様でよい」となり、ホールドタブ側の複雑な仕組み（`KeyConfigModal.tsx`の`HoldPanel`のTD関連ロジック一式、`protocol.ts`の`findFreeTdSlot`、`App.tsx`のtdSlots配線）を丸ごと削除した。Mod-Tap/Layer-Tapの通常機能には影響なし。
- **[x] `scripts/build-firmware.sh`にRP2040自動ビルドを追加**（本人依頼）:
  - 専用QMK worktree `~/qmk_firmware-keyball-rp2040` を新規作成（`~/qmk_firmware`と同じコミットの検出ヘッド、既存の`qmk_firmware-keyball-link`/`-keyball-plus`と同じパターン）。
  - パッチ3本（`keyball-rp2040-firmware/patches/`）を適用。このうち3本目`0003-rgblight-breathing-respects-brightness.patch`（呼吸エフェクトの明るさ反映修正）は、実は今まで`~/qmk_firmware`の作業ツリー上の変更としてのみ存在し、**一度もパッチファイル化されていなかった**（`git worktree`は新規作成時にこの種の未コミット変更を引き継がないため、今回このことに気づいた）。`git diff`から新規に切り出してリポジトリに追加し、恒久的に再現可能にした。
  - submodule（chibios/chibios-contrib/pico-sdk/printf/lufa）は`git submodule update --init`がGitHubからのフルクローンで数分以上ハングしたため中断し、代わりに`~/qmk_firmware`の既にチェックアウト済みディレクトリを`rsync`で直接コピーする方式に変更（大幅に高速）。この手順をスクリプト冒頭のコメントに明記した。
  - `keyball39`・`keyballplus`のRP2040版（`.uf2`）をビルド・`public/firmware/`へ配置する処理を追加。RP2040版は通常版/LED版の分岐が無い（統一ビルド）ため、AVR版と同じ「取り違えチェック」は適用できず、代わりに「空でない」「異常に小さくない（100000バイト未満はNG）」の検証にした。
  - スクリプト内の全角括弧を変数展開に直接続けて書いていた箇所（`echo "== $kb（RP2040版） =="`）で`kb: unbound variable`エラーが発生（非UTF-8ロケールでの多バイト文字とシェル変数名解析の衝突と推定）。変数展開と全角文字の間に半角スペースを入れる形に統一して解消。
  - `npm run update-firmware`をAVR版（keyball39/44/61）・RP2040版（keyball39・keyballplus）すべて通しで実行し、成功を確認済み（整合性チェックも全項目OK）。
- **[x] Keyball+のAVR版配布を停止**（本人判断。「Keyball＋はrp2040版のみリリースとし、AVR版は非表示としてデータは取っておいてください」）:
  - 上記の自動ビルド検証中、AVR版Keyball+ LED版が「The firmware is too large! 28806/28672（134バイト超過）」でビルド失敗することが判明。`keyball-plus-firmware`側の修正はせず、AVR版自体の配布を打ち切る方針とした。
  - `build-firmware.sh`から`PLUS_SRC`/`PLUS_QMK`（keyball-plus-firmwareのビルド手順一式）を削除。**`keyball-plus-firmware`リポジトリ自体・`~/qmk_firmware-keyball-plus`worktreeは削除していない**（本人指示通りデータは保持）。
  - `FirmwareFlasher.tsx`: `BUILTIN_FIRMWARE_AVR`/`BUILTIN_FIRMWARE_AVR_LED`から`keyballplus`エントリを削除（`Partial<Record>`化）。機種選択リストが`BUILTIN_FIRMWARE_AVR`のキー一覧から生成されていたため、これをやめて`MODEL_LABELS`から生成するよう変更（そうしないとKeyball+自体が選択肢から消えてしまうため）。AVR/RP2040タブは「選択中の機種にそのchip版のビルドが無ければ無効化」という対称的なロジックに統一（今まではRP2040側だけこのガードがあった）。存在しない方のchipが選ばれていた場合の自動切り替えも双方向対応にした。
  - `firmwareFeatures.ts`: `LATEST_FW_VERSION.keyballplus`を、AVR版`keyball-plus-firmware`のバージョン体系（1.0.1）から、RP2040版`keyball-rp2040-firmware`の`kb_version.h`（0.1.0）に一致するよう変更。**この修正をしないと、RP2040版keyballplusは常に「1.0.1より古い」と誤判定され「要更新」警告が出続けるところだった**（AVR版配布停止に伴い見つけた副産物的な不具合）。
  - **既知の未解決課題（今回はスコープ外、参考記録）**: `keyball39`はAVR版・RP2040版の両方を配布継続しているが、`LATEST_FW_VERSION.keyball39`はAVR版のバージョン体系（1.3.0）のみを表しており、RP2040版keyball39（0.1.0系）接続時はこの新旧比較が正しく機能しない。chip種別ごとに比較対象を分ける改修が必要（`firmwareFeatures.ts`にコメントで明記済み）。
- `npx tsc --noEmit`・`npm run build`成功、`rp2040-dev`プレビュー反映済み。**実機での最終確認はまだ**（本人に、ホールドタブから「ジェスチャー」「精密モード」タブ自体が消えたこと、FirmwareFlasherでKeyball+選択時にAVRタブが無効化されRP2040版のみ選べること、を確認してもらうこと）。
- **mainブランチとの統合（本人依頼の推奨案）**: `main`が分岐後に10コミット独自に進んでいる（AVR側のトラックボール誤動作修正・マクロ機能拡張等）ため、単純なfast-forwardは不可。推奨手順: (1) `rp2040-dev`上で`git merge main`を行い、コンフリクト（`protocol.ts`/`keycodes.ts`/`presets.ts`/マクロ関連で発生する見込み）を解決 (2) `npm run build`・`npx tsc --noEmit`・実機一巡確認 (3) 問題なければ`main`を`rp2040-dev`の内容にfast-forwardマージ（`rp2040-dev`は今後も開発ブランチとして存続、リリースの都度`main`へマージする運用）。リスクの高い`main`への直接作業ではなく、低リスクな`rp2040-dev`上でコンフリクト解決してから昇格させる流れにするのがポイント。**未着手**（本人の実行判断待ち）。

---

## 5. 主要ファイル
| ファイル/ディレクトリ | 内容 |
|---|---|
| `src/App.tsx` | 中心。接続状態管理・タブ切り替え・footerのバージョン表示など |
| `src/hooks/useKeyball.ts` | WebHID通信のラッパーhook（接続・キーマップ取得・各種設定の読み書き） |
| `src/lib/hid.ts` | WebHID低レベル通信（`KeyballHID`クラス） |
| `src/lib/protocol.ts` | HIDプロトコルの型・コマンド定義・バージョン比較関数 |
| `src/lib/firmwareFeatures.ts` | 機種別の最新ファームウェアバージョン・機能フラグ |
| `src/lib/avr109.ts` + `ihex.ts` | ブラウザから直接書き込むためのAVR109プロトコル・Intel HEXパーサ |
| `src/components/FirmwareFlasher/` | ファームウェア書き込みUI |
| `src/components/SettingsTab/` | LED・トラックボール・マクロ等の設定タブ群 |
| `public/firmware/*.hex` | 配布中のビルド済みファームウェア本体 |
| `wrangler.jsonc` | Cloudflare Pages設定 |

---

## 6. 開発・確認のやり方
```bash
cd ~/keyball-configurator
npm install          # 初回のみ
npm run dev           # 開発サーバー（http://localhost:5173）
npx tsc --noEmit       # 型チェック（ビルド前の確認に）
npm run build          # 本番ビルド（dist/に出力）
npm run deploy          # ビルド＋Cloudflare Pagesへ本番デプロイ
```

---

## 7. ハマりやすい点メモ
- 対応ブラウザは **Chrome / Edge のみ**（WebHID・WebSerial非対応のSafari/Firefoxは不可）
- ファームウェア書き込みは、機種によって「通常版」「LED版」の2種類あり、**hexファイル名の末尾 `_led` の有無**で区別している（`FirmwareFlasher.tsx` の `BUILTIN_FIRMWARE` / `BUILTIN_FIRMWARE_LED`）
- LED版は容量の都合でマクロ・ジェスチャー等が使えない機種が多い。機種ごとに正確な差分が違うので、UIの案内文言を変更する際は各ファームウェアリポジトリの `rules.mk` を確認すること
